/* Write a new arrow function `average` with an array as single argument
* calculate an return the average of all the values in the array
* */

// solution
// const average = arr => arr.reduce((a,b) => a + b, 0) / arr.length

average( [ 1, 2, 3, 4, 5, 6, 7, 8, 9 ] );